# Base URL of the Swarna Prabha API. In dev the Vite proxy also forwards /api,
# /uploads and /jewellery-images to this target. On Render this is the API web
# service's URL.
VITE_API_URL=http://localhost:4000
